#include <stdio.h>
#include "main.h"
/**
 * print_most_numbers - prints most numbers
 * Return: void
 */
void print_most_numbers(void)
{
	int c;

	for (c = 48; c < 58; c++)
	{
		if ((c == 50 || c == 52))
		{
			continue;
		}
		putchar(n);
	}
	_putchar(10);
}
