#include <stdlib.h>
#include <stdio.h>
/**
 * main - prints a new name
 * @argc: character used for counts
 * @argv: character used 
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
